<Window x:Class="WPFApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFApp"
        mc:Ignorable="d"
        Title="WPF App" Height="450" Width="800">


    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label FontSize="20" FontFamily="Bahnschrift">Name List</Label>
        <ListBox Grid.Row="1" x:Name="lstNames" FontSize="20" FontFamily="Bahnschrift"/>

        <StackPanel Grid.Row="1" Grid.Column="1" HorizontalAlignment="Right" Width="385">
            <TextBox x:Name="txtName" Height="40" FontSize="20" FontFamily="Bahnschrift" Background="White" />
            <Button x:Name="btnAdd" Margin="0,5,0,0" Click="ButtonAddName_Click" Height="40" FontSize="20" FontFamily="Bahnschrift" Background="#FFFF6060">Add Name</Button>
        </StackPanel>

    </Grid>
    
    


</Window>
    

